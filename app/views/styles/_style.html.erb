<% plain ||= false %>
<div id="style-<%= style.id %>" class="panel panel-default panel-want">
  <%= link_to_if(!plain, style_path(style), remote: true, class: "show-want") do %>
  <div class="panel-body">
    <span class="absolute"><%= icon "tags" %></span>
    <div class="offset-small">
      <%= style.name %><br>
      <span class="small">
        added <%= local_relative_time(style.created_at, type: 'weekday-or-date') %>
      </span>
    </div>
  </div>
  <% end %>
</div>