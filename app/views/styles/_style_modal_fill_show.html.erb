<h2><%= style.name %></h2>
<% if style.comment.present? %>
<blockquote>
  <p>
    <%= style.comment %>
  </p>
</blockquote>
<% end %>
<ul class="want-details-list">
  <% if style.size.present? %>
  <li>
    <%= icon("sliders") %>
    <div class="offset-small">
      <%= style.size %>
    </div>
  </li>
</ul>