<%= link_to 'Current User', current_user,
      class: "btn btn-default btn-lg" %>
<p>Home Page (Friends, etc)</p>
<% @friends.each do |friend| %>
    <%= friend.name %>
<% end if @friends %>

<%= @friend_array.inspect %>