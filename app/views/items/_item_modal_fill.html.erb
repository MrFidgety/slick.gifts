<div class="modal-dialog">
  <div class="modal-content">
    <div class="modal-header">
      <span class="pull-right">
        <% if @want_view_access == "edit" %>
        <button id="edit-want" class="btn btn-default">
          <%= icon("pencil-square-o") %>
        </button>
        <% end %>
        <% if @want_view_access == "view" %>
        <button id="purchase-want" class="btn btn-default">
          I've got this!
        </button>
        <% end %>
      </span>
      <h4 class="modal-title">
        <span class="absolute">
          <%= icon("barcode") %>
        </span>
        <span class="offset-small">
          Item
        </span>
      </h4>
    </div>
    <div class="modal-body">
      <div id="show-want-section">
        <div id="want-details-section">
          <%= render 'items/item_modal_fill_show', item: item %>
        </div>
        <div id="want-purchases-section">
        </div>
      </div>
      <% if @want_view_access == "edit" %>
        <div id="edit-want-section" class="start-hidden">
          <%= render 'items/item_modal_fill_edit', item: item %>
        </div>
      <% end %>
      <% if @want_view_access == "view" %>
      <div id="purchase-want-section" class="start-hidden">
        <%= render 'purchases/new', item: item %>
      </div>
      <% end %>
    </div>
  </div>
</div>